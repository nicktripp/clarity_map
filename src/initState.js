import nodes from './data/nodes.js'

const INIT_STATE = {
    mapStyle: null,
    popup: null,
    nodes: nodes.nodesList,
    geocoder: null,
    searchResults: null,
    highlightedPoint: null,
}

export default INIT_STATE;
